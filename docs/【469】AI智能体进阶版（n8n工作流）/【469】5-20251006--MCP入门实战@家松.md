> 提取时间：2025/10/18 14:29:06

哈喽哈喽大家晚上好，中秋快乐，今天晚上刚好是中秋节，大家能听到我的声音是吧，我看现在直播间里有 17 个人，那我们先先开始啊，我们先等着点，今天晚上的主题是 MCP，那么 MCP 跟那个 N8N 这些，他们其实有一定的关联，然后呢在讲 MCD 之前，先跟大家摸个底，然后因为大家之前应该参加过几次直播了，还听过其他老师讲的直播，我想了解一下大家现在应该是 19 人，19 人的话，大家嗯基础水平是怎么样啊，咱们这次的那个智能体的一个班，是一个高级进阶版，然后大家之前有没有什么编程基础，有没有什么编程基础嗯，有过编程基础的打一，没有编程基础的打二，之前没有做过，没有接触过编程的，大家可以互动一下，因为这个决定了我给大家讲的内容，的一些深度嗯，前几天我在群里，在群里经常看大家开赢了很久了，好几下了，然后还在卡在前面，卡在前面那个 N8N 怎么安装的情况，对班怎么安装，然后那个有些人怎么配置，然后一直说网络问题，有梯子问题，然后那个 docker 怎么安这些问题，看了纠结了好久，我看现在回复的大部分人现在是回复了，回复的都是一，都是有编程基础的对吧，那就没有没有知晓的行，那我前面没有没有打二的话，我前面就就稍微带过一下，因为我前两一叶知秋，应该是一叶之秋，应该是小波老师吧，小波老师那个那个头像，我看应该是有两个打二，OK 我那我前面简单介绍一下吧，还是把那个 N8N 这个安装这块介绍一下，因为我看在群里面之前，还是有同学一直卡在第一个环节，就是这个 NN 怎么安装，卡了好久，首先是那个咱们那个教练，咱们教练其实整理的内容非常全，所应用非常全的话，他是给大家讲一个 8N 安装 docker，在 docker 下怎么安装的话，那 docker 安装的话，它其实整体来说难度稍微大一点，因为他这个 docker 环境是一般我们生产环境，就是正式环境，一般会用 docker 这种方式去做，但是呢如果大家是没有编程基础的，像前面有很多打一的所有编程基础的，那他去了解他 docker，然后使用 docker 的话应该会非常非常快速，非常快速就能上手，但是没有没有编程基础的，没有这个概念，然后用 docker 的话上手就比较慢，那如果之前大家用 docker 什么的，还卡在那个环节，就建议大家用其他的方式去安装把，为什么呢，因为呃我们可以快速的先去使用起，先使用起来了解啊，这个 N8N 这个智能体你真正熟悉之后，再回过头来看看啊，docker 是怎么安装的，那么包括后面有很多进阶的 K8S 这种，稍微复杂的一些些环境配置是怎么配置的，那我这里面大家可以看一下那个飞书里面，08N 里面，这里面有一个那个 node js 的安装方式，那 node js 的安装方式呢，首先是呃我这是以 windows 系统为例啊，大家如果是啊，那个苹果的系统下面也有包括 LINUX 手班图的系统，前面有介绍，那像那个 windows 系统的话，大家还是可以先访问 node js 的官方网页，这个这里面有官方网页地址，先把这个这篇文章之前我做的，这电直接显示出，这个大家可以参照这个前面前面这个这个环节，其实用 node js 的方式安装的话会非常非常便捷，非常简快，但是你如果是 LINUX 系统，LINUXLINUX 系统的话，就就就没有那么方便，那如果是大家在练习的情况下，特别是 windows 系统下，你就用这种方式最快速启动，包括 docker 不会用的情况下，你用这种方式会尽快，比如说先到 node js 官方网页下载下载，这 node js 刚才应该改这边啊，进到这个 node js 官网里面都会，这里面你选了 windows 系统是吧，64 位的直接下载给安装包，下载完之后安装就可以了，node js 安装安装完 node js 安装完了之后，下面有一个什么嗯，程序安装安装完之后呢，就是环境配置，你就参照这个环境配置啊，环境变量已经配完了，然后呢再去命令行里面敲一下 node node v 这个版本，配下版本就可以，啊我看到 node js 这个版本，这就表示安装成功了，安装成功之后，第一步非常简单，第一步就是下载 RENODEJS，然后安装到本地，那第二步呢就是安装 N8N 两种方式，NPX 方式和 NTX 混动方式，这个是临时性的大啊，不需要用这个，用下面的文件就是 NPM1store n8N 杠 G 啊，这个命令是在哪敲呢，也是在这个 CMDCMD 命令行下，命令进行属下去是吧，那也可以也可以，Part shop or sh，大家如果是新的系统里面都会带高抛 tr，或者是这个这是替代的 CMD 命令行，都可以在这上面去运行这个命令，进到这个命令行里面去复制去执行啊，这样的话就可以啊，安装完安吧，这个安装可能会有段时间，因为它有点大，一点几不进，安装完 N8N 之后启动也非常便捷，确定，直接上答案，直接答 N 类型就可以结束，或者是答案 start start 都可以，你看一下这里面只要敲正命令，前面前面这些步骤都完成之后，执行这个命令，那就他就可以启动了，启动之后这个地址是 LOCALHOST56786 端口，这是非常快，这种方式是非常便捷的，这是我之前的，我本地的一个班，另外的是这个这个汉化画画的话，大家如果图简单一点的话，就是安装一个浏览器插件，这种浏览器插件就可以显示英文，还是显示翻译的，直接自动全部翻译，嗯声音还有点卡吗，因为我这个是在老家，我这在老家的话用的是 WIFI，八点了吗，声音还卡不卡，那这个的话其实常见问题啊，这个 N8N 在 windows 下面，这个这种方式会比较快，这种方式就比较适合我们新手，新手去做一些啊使用啊测试啊，包括一些那个快速的试，快速的入门，快速上手，用这种方式就行，不然你我看大家卡那个 docker 安装，卡了好久，特别纠结那个 docker 的问题，这里面还会介绍一下其他一些环境问题，比如说 windows 里面如果缺少 CPK，其实就参照这些解决问题的方式，这个环节，噪音噪音噪音是我这边开着风扇，在老家没有开着风扇，我把那个风扇离远一点，这种方式大家可以试一下啊，然后现在是 30 多个人，然后那个如果之前纠结 docker 的问题，卡在 docker 环境的，可以用一下这个这个 windows，这个直接 node js 的安装方式，现现在还有噪音吗，现在还有噪音吗，没有的话，我读继续了，然后对，然后大家如果 OK 的话，这个这个就先借过那个简单过一下，因为我看之前大家有纠结那个地方卡了不少，卡了挺长时间，然后呢回到我们今天讲的那，今天讲的话主要是讲这个 MCP 快速入门呃，为什么讲这个呢，因为我们可以看到，首先我们从从那个 N 班的这个工作流里面，随便打开个工作流之前创建嗯，随便打开个工作流里面，比如说我们介绍一个消息之后，这边有一个 A 证的智能体是吧，这里面大家会看到右下角，右下角这有个工具对吧，工具这里面会可以哎，可以配置这里面可以配置 CP 对吧，就是 MCP 的客户端连接连接到服务端，如果你后台起了一个服务，然后呢这个智能体零八也好，比赛也好，这种智能体工作流，那可以去连接你后端的 MCB 服务，那为什么要去连接呢，就是很多功能，比如说我们很多功能大模型，它不能嗯自己解决的，那我们可能需要借助一些什么外在的一些工具，这些工具怎么去弄呢，以前传统的方式是什么呢，比如说啊我去起一个 HPP，自己写一个写一个接口对吧，然后呢想办法让大模型啊，你调用大模型，然后大模型访问信息之后，我再去啊调用这个接口，但是呢接口的方式就在于这个协议啊，或规范不统一啊，CP 呢是我们形成一个统一的规范，也是类似于接口的形式，但是呢它可以有一个统一的标准，然后扩展了一些我们我们的一些功能，大模型些功能扩展什么功能呢，我们可以类似的参考下，比如说我们可以看 EXCE，这个网速稍微一点点大嗯，就是拿 WIFI 的，请问下这个线官网，OK 我把那个我把那个共享卸一下，大家刚才应该没看到这个工作流是吧，我在我在就是我们正常一个工作流里面，这个 AI 啊，agent AI agent 里面右下角有个工具对吧，这个卖工具的话，我们可以配置 MCP，这个 MCP 的话，其实就是去完善智能体的一些功能，比如说智能体里面缺了一些功能，比如说比如说那个查询天气，或者是查询当前时间这种啊，需要这些工具，我们就可以自己去开发，自己开发完之后呢，集成到的 N8N 也好，或者智能体也好，其他的比赛也好，其他智能体都可以，那以前我们是嗯没有 MCP 协议的时候，我们就是用传统的方式，就是写一个接口，然后那个大模型或者是用代码去调用那个接口，然后再把这个接口的信息返回给大模型，然后拿大模型整合这个信息给出答案对吧，你可以看一下这个你举个例子，Lipsic，举个例子看看，比如说我们去掉联网搜索，就说我问一下大模型，今天深圳喷气，正常情况下它是没有办法，他只能是去网站上搜索一些信息，但是呢他如果没有工具集成的话，你看这都是查的网页，但是呢它不一定是查到今天真实的信息，10 月 6 号，这是这是从网页上面去查到的，就现在是这个智能体，这个这个 DPC 格它经集成了翻身靠代码，代码调用的功能，像早期的话，他是没有办法查出找最新的天气是什么情况，包括问一下今天的现在的记录时间，多少，看看他能不能给出来，啊，嗯这个呢，是因为它现在已经集成了一些一些功能，甚至是查询网页是通过网页搜索，是一些信息获取啊，能获取到，但是呢像这种嗯查询系统时间这种功能诶，TIPSK 他说现在没有办法没有办法给到，那怎么办呢，就可以集成一个 MCP 的一个工具，让大模型去啊查询这种信息的时候，让大模型去查这个工具，然后把这个工具调用完之后，给到的信息给到大模型，然后大模型再汇总出来，给出我们标准的一个信息答案，嗯这块前面，那我回到我知道了 MCP 能干嘛用，然后呢其实刚才是比较通俗的一些解释，然后呢这里面给大家看下这些标准性的解释，MCP 是什么，就是模型上下文协议对吧，简称 CP，它也是通过大模型和外部数据源工具之间，一个交互对安门主要的一些架构，比如说 mc d host plt server，这个的话偏技术一点的，懂代码的应该都知道什么意思啊，就是一个客户端，一个服务端，包括一些本地的数据源，包括远程的一些服务对吧，架构组织是这样，它的核心功能呢就是一个标准化的一个接口，包括上下文的保持，包括模块化设计，动态发现双向通讯就行，那这里面另外介绍了一个 function colleague，就是刚才提到的，如果大模型有一些功能没有办法啊，没有办法实现，我需要外在的工具，怎么去使对外在的工具实现的时候，那需要用到这个方身 coin 这个这个这个功能，那像大家应该知道像 DPC 和 R1 早期的版本，它也是它是不支持 function call in 啊，随着后来一些版本更新了支持的 fction column，function column 什么意思，就是我把那些工具接口信息，以标准化的一种形式提示词给到 DC，给到大模型，大模型能返回的标准标准的输出，告诉我应该调哪个工具，然后传什么参数啊，然后呢我拿到这些信息，然后去执行，就相当于让大模型具备了执行方法功能，那这个图是我在网上找了个图上去克里呃，讲一下它的一个流程，比如说用户发送了一个信息之后，他会去根据这个信息来判断调用方法，判断是否需要调用外部函数，如果需要调用外部函数的话，就是把这个信息输出出来，整合出来后呢，调用外部的那个传递给外部的一个函数库，外部的函数后呢，再根据这个流程是吧，根据下函数定义啊，参数啊，包括从那个工具列表里面获取，获取出有外外部函数的路径是什么，方法，请求参数是什么，一步一步执行完，执行完之后呢，再把这些信息追加给大模型，大模型，大模型的信息，那大模型本身的内容加上啊，外部工具返回的信息整合到一起，再二次二次加工，再调一遍大模型，大模型把这个信息整合之后，给到终端输出，就像之前的话那个如果是不需要放生框里，就是直接就输出大部型的一个信息，所以我们看到就是嗯，如果我们在真正的企业级应用的时候，无可避免，就是说我们除了像这种 N8N 里面，直接配这种工作流对吧，直接配工作流，然后直接去配这些信息，这些插件工具的话不可避免的，有些工具可能市面上没有，或者是只适用我们自己内部的需求，那怎么办呢，就要自己开发开发完这个开发完这个工具之后，最好把包装成 MCP 的一个协议，然后呢让大模型直接就可以去调用这个 MCP，然后直接整合这个信息，OK 然后呢概念性的东西应该给大家讲了一下，这块大家看看有没有什么问题，大家可以先提问题，然后我再给大家补充一点点信息啊，就是我们在在看这个，智能体，大家都说智能体像 N8N 这种，那市面上智能体应该有两大类，应该叫两大类，一种是像 N8N 这种叫什么呢，叫 workflow，workflow 形式的智能体，这种智能体的形式是什么意思呢，就是哎我有明确的流程看，大家可以看到第一步要做什么事，第二步要做什么事，第三步要做什么事，要有明确的流程，以流程工作流的形式配置在这里，叫 workflow 的智能体，包括这个工作流程里面每一步配什么插件是吧，配什么记忆啊，或者配什么工具都可以，那还有一种大类的叫什么，叫叫 reac react 形式的智能体，以 act 形式就能给的就是什么呢，就是类似 auto dream 的那个模式，就是呃我后面有很多工具，很多 CP 的工具，比如说嗯查询天气，直接调用大模型，然后呢什么查询地图是吧，然后查询各种信息，各种工具，然后呢我去给大模型提问，大模型自写的判断，我应该调哪个哪个工具，调哪个 CP 自己去规划啊，看到那个谁小时，那个小石学长写的那篇那个教程帖里面，有没有写到对吧，计划执行，根据这个形式，然后呢不断去去自自主规划的啊，这种叫 react 形式的，这里来给大家补充一下，就是不错，是，这样，这两种形式的正中距，前面的定义部分应该大家比较好理解是吧，AE 进入到正式的内容就是 MCP 客户端，刚才讲的，刚才讲到了是吗，我们分为几个核心部分，然后呢，主要主要核心部分在这，MCB 的客户端和服务端，客户端呢就是嗯我们先先有一个服务，后面那个服务起来之后，我们我可以自己写一个写一个方法去调用，调用那个后台的服务，那我们就要客户端，服务端的话就是提供给这个服务，然后大家都可以用这个服务对，然后呢，首先这里面先讲了 MCD，客户端开发里面会讲一个 UV 工具入门指南，UV 工具的话是 Python Python 的一个依赖管理工具，类似于 P 和康纳，这个大家有没有了解，这个答案就是这，嗯可以啊，刚才刚才的这个黑书文档，我这些文档都是这个内容，MCP 快速入门的实战，是那个大家手里看到的那个文档，就是这些内容，这只是我单独列出，然后刚才还有一个还有一个这个嗯，这个这个入门的这个推数，就这个当入门的这个非常简单，这个这个也共享出来，大家也可以参考去学习，哦对用户开发过程嗯，有一些工具还是还是要自己去开发，比如说真正到企业级用，因为大家可能现在接触的比较多的是 C 端应用，就是可能是个人使用的一些应用，那大家去用网上别人提供的方法，别人提供的工具都没什么问题，但是如果是低端的，那可能有很多东西要自己去实现，甚至有可能说这个功能非常简，但是呢居然把非要复现一种拿法，在本地去实现这个 MCP 客户端和服务端，为什么就是保证它的数据安全，避免你的数据，特别是一些数据传上去之后，它 pd 给你保存了是吧，他那个接口后面就给你保存，给你怎么窃取了，这就然后呢，这个 UV 工具呢就是嗯 Python 里面的一个依赖管理，大家如果用 Python 的话，应该会比较熟了，如果你没用 Python 的话，那也没有关系，Python 不熟的话，你跟着就好了，像我们之前安装一些依赖包，可能会经常用到 table con 达对，然后呢用 UV 也可以按 UUV 的话，本身还是先有这个这个这是 UV 的安装流程，这个安装流程的话就是先给大家随便看一下，这个是我本地的那个拍 charm 编辑器编辑器里，这个我是呃，我原来是用框架的框架，然后激活激活一下你的那个环境，那我就激活到这个 solos 这个环境，就我目前经常运用的虚拟环境，如果大家在我现在讲这个环节听不大明白，说明大家那个基础的 Python 就会都是，可能不是那么熟，我需要注意一下这个词，这块都是一些基础的 Python 名字，比如说这里面，然后在这个环境下面去去安装这个 UV，就这瓶是多少年，因为之前之前已经安装过了，所以它其实已经存在了，啊这里面会介绍一些那个 UV 的基本用法，比如说它可以像 pp 一样使用是吧，安装那个 Python 那些依赖 UV 什么什么什么，这个 UVUV 激活一些虚拟环境，创建虚拟环境，激活虚拟环境之前都是用康纳是吧，这个用 UV 也可以包括呃安装所有的依赖文件，用依赖文件去安装，所有都可以包括直接运行速效这块的话，前面这块 UV 的使用也是基础的东西，要求借过一下大家知道的，OK 然后呢，真正的就是到 MCP 这块，CP 创建客户端流程这块就正式开始，比如说我们可以先用 uv it 是吧，创建一个，这是我我的自己的目录啊，比如说我自己的所有的 Python 文件都会放在 space，Python 下面，然后创建一个 mc d client 这个项目，使用这个命令去创建，然后呢进入到这个命令下面，进入到这个项目下面，这两个代码是这样的，然后呢啊 MCP 客户端的虚拟环境，包括进入这个 ENVVNV，进入这个项目录，包括执行激活这个项目环境，包括这些激活环境，source 啊，这些这个点，因为 v e n v in activity 跟那个宽达类似，这些是常用的一些些命令，前面这块讲的比较快，如果比较基础，然后呢，比如说我们这个项目已经创建好了之后，就看我们之前这个项目已经创建好了对吧，CD client 项目结构，就这样一个结构，那安装安安装 MCP 就是 UVIMCP，之前的话这个依赖包了，这是依赖包，像我们之前用 qua 和 pip 的话，就是 pistop stop CPU 或者是 cha stop CPU，UV 的话就是 UVNCP，然后呢整个项目里面创建完之后啊，就会把这个依赖包加载进去，大家可以看一下这个底下这块，底下这块这个命令方块，滚蛋，大家可以看到这块吗，先进入到这个环境，激活这个环境，激活这个环境之后呢，然后 uv ADD cp 这把这个依赖包下载下来，看一下稍微看一下哦，好不好意思不好意思，我又那个了，这个项目能看到了吗，m CD client 对吧，这个 Python 这个项目创建完之后，大家可以看到这里这里面，然后呢去就是激活环境，激活环境，然后呢然后把这个依赖包加进去，跟大家一起操作一遍吧，记回答题，听到热环境下 UV，电脑出现一趟过的，但是有提示已经存在了，看清楚这两步了吗，就是进到这个项目，进到这个环境，进入这个项目的虚拟环境之后，这个包加进去，CP 的一个包加进来，那你按 CP 包加进来之后，这些引用和 from m cp，Import plant，Session，Studio，Service，PERMETER 这些，那这些引用就正常，不然的话你可以引用是报错的，看下大家这一步有没有跟上，有什么问题，废了，大家可以也一起进行进行一些操作，可以进行一些操作，因为只需要 copy1 些 copy，不要 copy 注释，就 copy 这里面的东西分你一步步执行就可以，然后呢，这里面给大家弄了一个快速开始的一个 demo，这个是比较简单的，就是一个模拟模拟，跟 MCP 的 MCP 的客户端模拟一个客户端，然后呢跟服务端进行交互这一个模拟，但没有真正的初始化服务，后端的服务端，大家可以把这个 call 代码 copy 下来，直接写到 Python 文件里面去，但是在这，这对就是这个，大家有号位进来吧，然后呢这我我先给大家演示效果，然后再给大家解解释一下，这个这个里面的代码是什么意思，或者是教给大家怎么去看这个代码，啊直接右键在我们的 pup，直接右键我们执行 run city client 啊，就这个这个 cp client py，这个 Python 文件就已经执行起来，那么在这个文件里面我可以看已经接入了，我就可以提问，进行交互，然后然后大模型大模型就已经返回信息了，然后这里面还是在思考过程的思考过程，因为没有关掉，因为我这个是是用的是那个，硅基流动的硅基流动的那个 API，用他那个大模型 API，然后那速度会比较慢的呃，我后面那些案例，我之后面那些案例都切到那个千分章了，就阿里云百炼模型，后面大家如果真正实消息，后面会给大家布置个作业，把这个把这个云百变呢切换成额，不是不是把那个硅基流动的切换成那个零百列，那硅基流动的速度太慢了，用起来觉得有点有点慢，没那么好用，现在我用的比较多的是阿里云盘链六个西，大家看到了吗，这里面就会来，已经会进行跟大模型进行互动模拟的客户端，然后跟进行互动进行说一些你是谁呀啊，你是回答是这个 SC 对吧，他们什么什么一些些一些分析回答出来，然后 OK 现在现在第一个问题，大家可能一开始看到这个代码，如果没有编程基础的同学，你看这个代码是不是就懵逼对吧，不知道要干嘛，虽然里面有一些注释，但是你还是知道的，这种这种情况下应该怎么操作呢，就是把这个代码文件直接下下载，比如说我是在这，这就是我这个代码文件，你把这个代码文件直接扔给大模型，扔给豆包，豆豆包是就是那个那个豆包是季节的季节，它有自己的 AI 编程对吧，然后呢我们就这样，一般是做两件事，第一件事就是给我解释一下，然后呢豆包就会给你分析这代码是干嘛的，有助于我们经常会有些问题，就是像我们这个训练营讲师可能给大家讲讲，这个代码，可以从头给大家讲一遍，每一行是干嘛的对吧，但是呢经常会有一些，如果你用了别人的代码，用了别人的工作流，包括也用了别人给你的 8N 的工作流，你知道干嘛，告腾是跟 8N 的工作流走是什么意思，也可发安那个工作流 JSON 文件的工业，让豆包给你解释一下，这是干嘛用，别看这，这段代码是基于 OpenAI API 的交互式聊天，客户端程序主要通过命令行跟用户进交，是在这里面，在命令行里面大就不用交互对了，我这里有个大金星，都，对这里面就会哎非常详细的详细讲解，这个这这个功能是整体的功能是什么，包括它依赖的是些库是吧，包括一些配置来源，你看到在代码里面还有，开始大家可能不懂是吧，这块这块是什么意思啊，其实他会他会告诉你读取这个文件这个参数，这个参数这个参数放到这里面来，这是其实是的，生产也不算完全标准，就是一般是真正做开发的，是通过这种配置文件形式的方式，把这些 KE 啊，URL 啊，包括模型名称啊，配置到里面去，不是像大家直接就是写死写固定的写在这的，OK 让让大模型给我们解释这些都是做什么用的，解释完了之后哎点点就读，知道什么意思了，你说音译的方法啊，比如说初始化的那个连接，Connect to work server，这个是这个这个是 process carry，这是表一个方法都会给你讲，这样利用大波形就是帮我们解除那些疑惑的，就知道哎我不用特别难懂，这个这个这个模型代码的代码，用理我让大模型模型出现，然后呢解释完了之后，还有一点就是你可以再完善一点，就是将上，代码，添加详细演示，假如说我们拿到别人的那个别人的代码，或者别人的那个工作流的 8N 文件之后，我们自己看不懂，然后呢解释完之后呢，当时记住了，后面记不住怎么办，就让大模型给我们添加详细的注释，每一个每一行代码是每一块特别详细的注释，这样我们就能看到不出事，我们知道干干嘛，问题对不对，这个我觉得是大模型的优势，特包括是 AI 编程的优势，就是诶他通过让大模型去帮我们思考，每段代码是干嘛的，帮我们增加上注释，这比我们自己去一点点去找，一点点去添加注释要快，对不对，啊这样的话就是哎整体的注释全部生成的啊，那里面井号前面的就是一些注释信息，包括还可以看构建 EPI 的消息列表，包括提示系统，提示用户当前查询，包括一些异步操作，告诉你这这块异步操作对吧，这里面每个注释都有对吧，包括 AI 的思考过程，返回思考过程怎么处理的，AI 最终的回答是什么，将当前的对话添加到历史，这些东西都是怎么做的，这样是不是我们自己也有办法去解决这个，AI 编程的问题了，第一个 demo，第一个 demo 非常简单，你如果前面环境都准备好的话，直接 copy 进去，然后去去执行，就能执行出来，看看大家有什么问题，对，可以你可以直接打开 tree，大家看，所以直接用雀雀也可以，这个工具也可以，但是但是我跟大家说一下，我的一些使用的一些一些感官吧，给大家说下我的使用感觉，就是我用 ch 的话，我感觉它比较好的话就是在于整体信息的浏览，然后呢整体信息的集成，包括一些代码都比较好用，但是呢嗯代码这块觉得在前端豆包，他的算力应该是比较足的，我感觉去的话应该是算力数线，你每次跟他交互的时间特别长，然后呢一些效果，我觉得豆包还反而感觉比吹捧一点，所以我如如果是一些简单的东西，如果就是整个项目不是特别复杂的话，我可能会用税额，有可能会用豆包和那个 cloud code，然后整体项目比较复杂的情况下，我才会用这 trap 垂直做，对可以可以，这些都可以，也不用也不用，大家都能读懂代码，但是呢，如果大家后期如果真正要去从事这个 AI engine，open cp 的开发，但是你得知道这个代码是干嘛用的，然后呢 AIAI 编程之后，你得知道哎他写的是不是有问题，要不要修改，要怎么改，得告诉他，然后那个第一个第一个这个 client 还是带大家过去，然他是整体做什，首先第一个是初始化，初始化是创建一个 speak 队列，然后呢这里面会有一些啊，前面是一些那个大模型调用，因为是调用 API 的方式，远程调用大模型，这里面从配置文件里面取 AAPIP，包括模型的地址，包括模型的名称，这块我稍微简约一下这个啊，因为我这块用的是硅基流动的，硅基流动的一些，有之前充过钱，之前充过，就是年初的时候充了钱，然后你刚好试试，就先用吧，然后这个用的是危机流动的那个 EPI，然后呢模型是 deep sk r1 模型，就是这个模型这个名称啊，它每一个每一个平台名字不太一样，你要把这个名称 model，这个要跟它平台里面的名称对应起来，然后呢 open API 的一个 key 在 BT 流动里面也会注册，之后你会有个 key，你把自己的 key 放到这里面来，这种好处是什么呢，就是这种模式的好处，就在于你如果换平台了对吧，因为首先模型调用这块，大家如果用的标准都是 OpenAI 的，这个调用方式的话，那这三个参数是固定的，好处就在于你后期换平台，比如说我想用嗯云买链，阿里云买链，那我只要把这三个值后面的改就可以了对吧，代码里面这块东西什么都不用改，这边把这个切换到其他平台，你就很容易切换呃，这个初始化的话，其实就是把一个是客户端初始化，另外一个是把那些大模型初始化，大模型的客户端初始化起来啊，这个是连接连接 MCP 服务，这块是做一个模拟，所以说暂时没有，然后呢，这里面是进行那些查询这种方式，代码去调用大模型查询，大家有没有用过，大概没有用过，对应该应该应该没我是吧，它呢是这种一些固定的格式，比如说前面里面是一个 list 对吧，一个一个列表，一个列表里面有个对象，对象里面呢是一个 JSON 格式的，这里面固定了，比如说有 role 角色，要有 content 内容，角色是系统，就是系统角色对吧，你会定义一个给一个给这个智能体，或者给个大模型，有一个系统系统提示词，你这类比较简单，就是你是一个智能助手，帮助用户回答问题啊，然后呢这里面 query 是前面啊，我们提的问题传入到这，这里面的特定的角色就是 user，就是用户提的问题放到这，然后把这两个合到一起，然后一起给到大模型，大模型就去调用 OpenAI 的 API 了，这这是 OpenAI 的格式啊，这不是 OpenAI，叫 OpenAI，是我的方式方式，后台其实调的是飞机流动的大模型对吧，飞机流动的 DPCC 大模型，然后呢来返回了，然后呢，这个因为这个 DC 个模型里面会有一些，带思考过程和最终答案的，所以这个你还要去提取思考过程，最终答案这块，大家如果后期真正开发的时候，也会有一点点注意，就是 deep sk r1 和千问切问三这种东西不太一样，就在于它是没有办办法关闭思考过程，因为 DPC 和 R1 它就是推理模型，所以它每次返回来的信息里面，是带思考过程和最终答案的，所以你要去把思考过程的最终答案去处理掉，就是因为你你可能只给客户看，只给用户看，那个最终回答可能不需要看思考过程，那你就要手动的去进行一些处理，你像前文三这种，它是可以开启和关闭思考过程的，那那种另外一种方式，所以这块有时候也会有些差异，OK 这个就是启动那个对话框了，就要求你输入的信息，这些信息放到这里面里面是清理资源，这里面是主函数，主函数启动，这里面是直接运行，所以这块这块是个简单的代码，CV 可 client 客户端的代码代有没有什么问题，现在四十四十七个人还挺多的，中秋节大家还在看直播，可以可以，但大家有问题了，一定要问，还是看不懂是吧，所以像这个像那个小波老师，或者大家那个我觉得咱们在这次训练营里面，一定是有有两拨人，一种是有一定的基础，有一定的基础的人，可能是想做智能体研发和一个 MCP 研发，做技术端的，对这些人呢其实他就对于这些人来说，可能这些代码其实就比较简单了，说他一看就知道什么意思，然后就算自己看不懂，然后把这个代码扔到大模型里面，大模型一解释就知道什么意思，那还有一拨人呢可能是 IB 数部分嗯，大家可能是想来看看嗯，智能体 MCP 能解决哪些问题，那我知道这个智能体的一些模式，先大概的逻辑是什么样，然后呢我再来一看这东西，哎我知道它能解决什么问题，将来我在使用的时候，在面对这些问题的时候，可以知道哪些是用智能体能解决的，这 MCP 解决的，哪些我需要找别人来找专业的开发来帮我开发，哪些可能是还是需要传统的技术才能解决的，这些，我觉得这两个大家可以就有这样的一个定位，如果是大家如果是不是特别有基础的，就是没有一点点编程基础的，然后呢，然后要做这个入门，其实这块面还是有点有点点有点点，因为这些逻辑性的关系啊，这些基础的东西解释起来比较比较复杂，它不是一两句话就能解释清楚的，但是这种这种情况下，大家建议大家可以先带着问题来看，就是你思考这个这个智能体，能给我解决什么问题，假如说我的工作中，我的一些业务场景中，智能体能不能通过智能体的方式解决，包含 CP 的方式解决，这样的话你有个概念再去思考，这个智能体究竟是我自己提升打基础去做开发，是找些人，找些专门的人给我去开发啊，你你有这个概念的时候，你也能评估出来，开发这个证明题大概需要多少钱，多少时间，这个录一个概念，我觉挺好，啊对对，五成程程老师说的很对，比如说用过工厂方法，就这个就又又复杂一点了，就是你要用设计模式，你要用设计模式的话，会兼容不同大模型返回值的处理问题，这个是非常好，但这个里面就没有去做，这里面就做了个 demo 演示，如果如果用设计，如果用设计方法设计模式的话，估计大家都看不明白，这块我就先这样，大家大家如果有问题一定要问，然后因为确定大家哪些点能卡住，趁这个机会跟大家在这样线上交流的时候，大家有卡住的点，有问题的点，大家可以提出来，然后我看我能解决，我给大家解决，如果我不能现场解决了，我给大家说一下解决思路，或者是怎么去解决，OK 然后这个文档我们就继续往下过，刚才应该演示了是吧，这是用 UV 的，用 UV 去执行，去去去启动这个这个 Python 文件对吧，包括你可以不用用用 Python，直接启动 CP 这个发展的规律，这些东西就是懂编程的，懂 Python 的，我一说就知道什么意思，可以看看运行效果，这个是没，这个是之前没接触大模型的，现在接入大模型的话去说，你跑的话就会大模型直接进行进行思考，这个回复了，这里边下面讲的就是我们在 MCD 的客户端里，就是模型，刚才是是只是用个简单的 m c t demo，是一个一个演示，然后这边是要接入接入大模型，比如说你是用 OpenAI 拍的大模型，这是第七个大模型，还是用千问三的 DMC，都会通过这种方式通过配置到配置文件里面，刚才跟大家讲的这点 ENV 文件，这个文件点 ENVNV 文件，然后里面配置这三个参数啊，对应的 T，那个 key 啊，UIL 包括模型名称，直到里面去，然后这里面会进行一个代码的修改，直接直接去查询大模型的这里的大模型，这个地址的配导，这就启动大模型，对调用大模型，调用大模型，这些方法都是固定的，因为你用的是别人的安装包，别人的包直接按这种方式去执行就可以，再去启动大家可以看到一些效果嗯，如果你配置了大模型之后，你再去启动它，就会你问他是谁的话，他会告诉你是一位助手中心，然后是由谁开发的，告诉你是 deep shaft 对吧，因为我用的 DC 模型，我就告诉你 deep sha，OK 这其实我们第一个非常非常基础，非常起步的一个阶段就已经搞定，就是我现在有个客户端，那我可以去访大模型了，那访大波型之后可以做一些其他的事，大波形进行互动，大家还记得我刚才讲的那个逻辑吗，大家还记得这个图，就是 MCP 的里面一样的逻辑，就是大模型去调用 function calling，他的逻辑是什么呢，就是用户先返回之后呢，先去接收到的请求之后呢，先判断是不是让大模型，判断是不是需要去调用外部函数，如果是调用外部函数，就是说的 MCP service 对吧，让我们去调用，如果不需要调用，直接回答，如果需要调用 CP 的 service 之后，他先去调用，调用 m CV service 之后，返回的结果给到大模型，再一次给到大模型，再次进行，再次进入一个思考，处理大模型这么处理，然后再返回给用户输出，所以如果是需要外部调用的时候，大家会发现它是三个步骤，就是大模型先进行第一步处理，然后呢外部工具进行第二步处理，然后呢第一步处理和第二步处理结果，合并起来之后，再给他们进行再次进行处理，然后呢第三次出的结果才会返回值，是这么一个流程，这图大家可以现在再看一下，这里面还有个扩展，这边还有个扩展，就是我们我们通常是调用自己用的话，就是可以调用线上的一些大模型平台，比如说云买链平台，飞机流动平台或者其他平台，如果是我们自己，如果是 to b 的企业开发的，比如说我们自己在建一些项目，建一些需求的时候，他必须可能是用自己内部搭建的一些一些造型，那他自己搭建的大模型其实也一样，只是那个搭建过程需要去另外去看，大家如果需要知道怎么去搭建本地的，就大模型私有化部署，那是另外一块内容，大家有需要的，到时候可以问我，可以给大家讲，就是一样的，就是你可能用欧拉玛对吧，欧拉马或者是 VLL 或者是 im studio 对吧，去在本地跑一个大模型等等，大模型跑起来之后呢，啊他的那个 OpenAI 的 API 风格一样，他也是 OpenAI 的 API 风格，只是那个地址是吧，直射地址可能变成 LOCALHOST 对吧，LOCALHOST 什么什么什么 V 什么对吧，再分一个后面路径，这个地址会变，然后呢按下 PIT 的话，应该不需要，因为你本地的话应该不需要，那不需要 K 或者需要的话，你可是按自己的配置可，所以这边也是一样的一样的文件，意思就是说你可以去连平台的大模型，平台的模型也可以连本地私有化部署的模型，这块是不影响你的和那个 MCP 服务的，然后这块我稍微跟大家扩展一下，大家可以看到这个 AI 整的对吧，N8N 这个工作流里面，这里面也是一样的，这个大模型也一样，这里面也是有 VIP 这个地址，也是我这个地址配的，就是那个阿里云买亚，也可以是云买店，也可以是你本地的私有化，私有化部署大模型，这样的话是不就串起来，就是我们班可以在本地本地搭本地搭建对吧，本地部署我的大模型也可以在本地部署，然后呢，我所有的东西全部是在本地一个一个闭环，进行一些直接调用，这种比较适合企业端，企业端对一些数据安全，数据要求有要求，一些信息有要求的，那我就全部是私有化部署，这样也是没有问题的，看你的需求场景，看你的需求场景，如果你是个人使用或者是测试环境，OLMOLLAI 欧拉马，欧拉马，或者说 v LLM studio，这两个都可以，或者测试环境是八，或者是啊有点，企业端企业生产环境的，用 VII，还有一个叫什么川 former，这名字有点忘了，是这两，为什么呢，为什么就是，现实情况没有大家想的那么简单，就是因为你本地的话，你本地的话，你一台电脑是一个单显卡，对不对，你用 4070 也好啊，40804090 都可以，没关系，你是个单显卡，但是生长环境一定是多服务器啊，特别是你如果线上使用的话，就是多服务器多显卡，比如说一台机器有八张显卡对吧，你可能要供出四五个机器那种的话，那个布拉玛和 VR，柏拉玛和 l x studio 这两个就不是就不行了，搞不定多服务多显卡的就需要用到 VLL，还有个叫什么什么 transformer，这记不清了，到时候给大家找一下，用那两种方式去复述，然后呢，嗯如果个人能力就是你动手能力没那么强的话，还想私有化部手话就用在 L4STUDI，凡是界面配置不需要太多的命令，因为 LSTUDI，OK 这块大家还没有什么问题，那我们下来接下来要讲核心的就是嗯 MCP，构建一个客户端，一个服务端，那我们举了个例子，然后这个例子其实第二场，这样大家可以从网上各种各种网上都能查到，就是一个 MCP 的一个天气查询服务器，server 的使用去构建一个天气查询的服务器，然后呢我在问大模型的时候，大模型就能集成我的这个天气查询的功能，我问他大模型今天天气如何，深圳天气啊，杭州天气啊，哎他就能调用我的 MCB 的一个客户端，然后去查询服务，查询天气服务，然后呢把当天的天气查询过来对吧，嗯这块的这块的定义什么就不念了，大家可以看一下，就是 resource 资源是吧，通过工具 from bts 就是提示词啊，这里面这个通讯机制，大家可以看一下嗯，MCP 的通讯机制，这个应该是这个写的不不全啊，应该底下应该还有底下还有这个，大家不用看看，底下就有啊，MCB 的三种传输方式，一个 studio，然后呢一个是 SSE，还有一个就是 string bosco GDP，然后呢这块也属于稍微进阶一点，稍微难一点的，就给大家讲一个是 ST，这个是这个方式的话，它一般情况下就是测试使用，因为他是弄一个子线紫禁城，然后呢双方就都是在本地，只有在本地一台机器上进行，这种方式的话可以，但通常情况下，你的客户端和服务端一般不是同一台机器，生产环境化一般不是这样，所以呢这种方式只是做的测试使用，那 SICE 的话也不行，SE 的话嗯，它是基于 HTP 协议的，但是呢有几个有几个非常重重要的问题，就是一个是不支持断线重连，你服务器需要长连接，一直连接，然后呢占用资源就比较大，然后呢包括一些基础设施兼容性限制，所以这种方式你现在基本上没人用，因为用的话出问题的话问题非常大，所以现在测试一般都是用第一种方式，然后生产的话就是用第二种方式，然后第第三种方式 string boh TP，但这一块有详细的信息，详细的信息就是这个 streamer dream boh TP p，它是怎么样的一个连接过程，大家如果是搞编程的是吧，搞那个经常参加过面试的同学，应该知道什么，三次握手是吧，几次握手啊，这个东西也是类似的，你看下底下这个顺序就知道了啊，几次握手，然后再怎么进行处理，然后返回什么什么信息啊，对那回回到这个啊，回到今天的主要的细节，然后讲这个这个编码啊，CP 有个天气查询的 MCD 执行流程，这是我网上找了个图啊，就是客户端发送一个请求到服务端，服务端进行响应，服务端呢进行发送请求到这个这个天气查询，API 请求完之后返回结果，然后服务端服务端呢，这里面这里面可能是由大模型代理，大模型来决定你去去调研，如何服务好呢，大模型，返回了那个方法之后呢，大模型进行处理，处理完之后呢，把这个信息再返回到类似这样处理，估计，那底下底下两块代码，第一个是 mc p server weather infort py，这个是服务端，服务端的一个信息查询，大家可以看下这个这段代码，嗯然后呢，这里面刚好有一个信息可能没给大家讲，这里面有一个要查询服务，当然也不是我们自己去弄这个天气了，对不对，我们也是调用别人的天气，别人的天气的那个 API 接口啊，我用的是这个 open weather，Open weather except，这个这个 API，这个是这个网站可能会比较慢，这网，嗯我看一下，这里面好像没给大家写这个注册的注册，大家看看是吧，有能力的话，根据这个地址就应该能找到这个网站，然后跟着，然后呢自己去注册注册一个信息，然后呢这里面会有一个 key 是 open weather 的那个 key，AKIK 你每个人注册完之后有个自己的，然后里面会有一些写信息对吧，把这个信息放到这，然后这段代码大家也可以 copy 到本地，比如说我这会就到这就行了，发配到这里，然后呢，这些信息这些信息也可以放到配置文件里面去，也可以放到这里面来，通过配置文件去读取，对不对，那后期如果你调用的不是 vocal visor，的那个接口，你可以在配置文件里面直接改，OK 啊，然后那个给大家，再给大家看一下，大家还记得遇到这种拿到这个代码之后，不知道什么意思的，看不懂，看不懂什么那个代码什么意思，怎么怎么办，怎么解决的，对不对，怎么解决，是不是把这代码下载下来，扔到购包这里面，推荐大家用豆包扔到豆包里，让豆包给你们解释一下这个代码什么意思，然后代码里面让让豆包给你把代码，添加详细的注释，就就能就能知道这个每一行说每一块，每一块的代码是干嘛用的，我就知道了，对不对，这块这块准确率还是比较高，大家可以放心的使用，继续啊，这里面简单过一下，前面是这个地址，该的地址在调用地方，调用方式在这，在这来进行去调用，下面是 open vior 的 APIP 值后底下的去获取，获取天气信息，应该会有一些些那个参数，参数要求是怎么样的，参数好了，包括这些，配置到这里面，然后呢 HTP 进行请求对吧，请求这个是 EPI 地址参数，包括请求头，包括超时时间进行去处理，然后处理完之后返回原始长原始的些信息数据，那如果数据超长呢，它可能会截断了，而日志里面进行截断，返回的数据不会截断，进行一些一些错误处理或是什么，第 7P 状态返回状态错误然怎么进行处理，然后超时进行些处理，然后几个兜底的处理，然后这个呢就是将返回的信息啊格式化，比如说他返回一个 JSON 是吧，JASON 之后进行传转化，一些转化，一些我们可以人类可以读懂的人对吧，这种说温度是什么样的，深度怎么样，风速天气，包括这个代码，这个代码大家也可以用 AI 生成的，比如说我们把那个返回的 JSON 直接分给豆包，然后让 JASON，让豆包把这个 JASON 转化成可以容易读取的版本，容易读取信息，然后再把人家写出，OK 这里面是一个核心的，大家可以看这个 MCP 的这个标签，加上 MCP 做啊，就这个就非常简单，就是你以前的如果是老项目，大家如果是一些企业级开发时，老项目，其实你改变基本上不用改这东西，只要在前面加一个这个标签 CP 头，然后呢这个信息就能获取到，就是就可以就可以去注册，就可以去使用这样 CP 这个服务就非常简单，就加一个标签，解下是启动，这是服务端，加了这么一个标签之后，那个嗯就就相当于说就把这个方法注册到那个，注册到这个 MCP 的列表里去了，工具列表里去了，然后呢你客户端去查询工具列表，然后我就知道哎我有一个查询天气的一个方法，那我可能还有一些其他的方法，比如说什么查询那个查询时间的，还有，长时间，对吧啊，这里面比如说这种是吧，查询天气的，这种是查询当前时间的，这样的话只要加入你的标签，只要加入你的标签 server，就你就可以把这两个工具注册进去，然后呢客户端就能获取到这两个工具的信息，这里面的话这个信息这个代码大家可以 copy 下来，进去之后自己去测试，去使用，客户端客户端的客户端一样，客户端的话，这块代码也可以把它复制到那个那个豆包里面，那到豆包给大家每一行增加一个注释吧，或者是详细的注释增加进来，然后我到这里来给大家解释一下，这个客户端的代码是什么意思，带大家过一遍，第一个 in it，第一的方法还是初始化对吧，初始化 GB 的客户端，然后呢这个是大模型的相关参数对吧，大模型的 K 大模型的地址，大模型的名称，游戏名称这块创建了，这就是把大模型的客户端创建出来，这个相关的散热信息啊，就这些信息创建，OK 然后呢这个是连接服务，连接连接服务的话，这里面可以让这个这个信息啊，这信息注册进来，哪些是 Python，先是 JS 的，如果是如果是那个，如果是那个 stud d I/O 的模式吧，第一个模式的话，这里面必须是加个这个就是他只认 PY 和 JS，如果是其他的没有关系啊，嗯 OK 大家可以看底下的，其实这里面有些注释了，比如说启动 MCP 服务器并建立建立通信对吧，这个代码做这个事，然后呢列出 MCP 服务器上的一些工具对吧，在客户端里面去把 MCP 的客户端，那个那个服务的服务端的一些工具全部列出来，一会儿启动的时候，大家都能看出这是这块的，下面这个就是进行查询了对吧，前端前面我们输入一个信息让他去处理，然后呢这里面就会接到的信息，接到信息，然后呢，也是把它封装成这个大模型的一个一个文本，大模型的一个啊入参参数，标注参数格式，把这个封装进来之后给到大模型，这里面还会要然后加一个这个，这个也是非常非常重要的，那这个就是 fashion calling 的一个注信息的一个，一个要求，就是你必须列 type 是 function function 里面有内容，有描述对吧，the sca 这个这这一些信息，然后放到这里面来，然后呢把这个信息给到大模型，大模型就能判断，你如果让我去查询天气，我就知道哎我现在有什么方法，我可以调 MCP 里面的，查询天气的这个这个 API，那他接下来就会判断，然后去执行执行你里面的呃这个工具查询，去查询结果返回天气相关的信息，这块就是如果里面有使用工具对吧，如果要使用一些工具，然后呢我就去执行执行这个工具，这是报道啊，就执行这个工具，我们把这些信息处理完，再拼接回大，再拼接回这个 message 里面，然后呢再把这些拼接的信息给到大，码数大幅进去，这里面 call2 套，这个里面它其实底层，底层本质的还是走 GTAP 而走 GDP，相当于可以看到这这里，相当于你知道他的地址之后，知道了那个请求的工具的方法和，工具的需要的参数，然后那个大模型把参数嗯，把工具名称和参数自己整理好之后，这里面就可以直接去调用工具，啊底层这个也是一样的，这个也是，开启聊天吧，开启聊天模式，进行一些信息处理，这个就是执行执行运行的所，那我先把这个底下的这两个解完，然后呢这个是讲那个创建的 client 和 server 的一个服务，那个创建，然后一会给大家演示一下，然后呢这里面先跳跳一下，这里就是大家可以看到，就是我刚才讲到 MCB 的三种传输模式对吧，一个是 SDDIO 这种方式，一个是 SSE，一个是个 STLGDP 这三种方式，然后我们现在用 stream stream htb，用这种方式，然后去执行，去执行，这块代码也一样，其实其实这块代码跟上面一样类似，只是说他的模式换成了，换成了那个 STRINGLEHCP 模式，这个是服务端代码，服务端代码，他的这个服务端代码差异主要是在这，一个是 m MCP server，客户端这块也会有点差异，客户端这块可以看到这，你用的是嗯 strbotp client，之前的不是给大家看，这个不是，这是 studio studio class，这个是之前是用这种方式，我们这个是适合还适合本地测试，就是你一台机器起两个进程可能用这种方式测，但是我们真正使用的时候只有这种方式，所以直接也是这种方式，要拿这块代码，大家可以把这个代码 copy 到自己的，自己本地，应该有那个地址，就是大家可以 copy copy 的地址，这个代码 copy 你去做这个执行，嗯启动顺序啊，启动顺序建议是先启动服务端，先启动服务端，再使用客户端，OK 服务端启动之后，这就是正常表示启动完成了是吧，本地 127.0.0.1 啊，然后 8989 这个端口，然后呢大家看下客户端，这块客户端如果是 HTB 协议的话，这个地方对这地方你看端口和 IP 要跟这个对应，要跟这个对应，如果和死的话，就是 6.7.0.0，这俩是一致的，啊然后大家再看一下日志这块，客户端日志的这块，大家可以看到是不是已经提示了，已连接到服务器，支持以下工具，一个是艾瑞 mother 就查询天气，一个是获取当前时间，这两个，这两个工具就是在我们这里的 server 端里面，注册的两个工具，一个是这个，对不对，加这个标签获取当前时间，Get current time，还有一个是查询天气，查询天气的方法，注册进来之后，呃这个这个写了个这个地方写了个测试的，测试是真正试跟没有关系，OK 我们先先问你一个问题啊，等起来，这对，大家还记得这，直接问 deep sick 的时候，说 deep seek 是不是没有办法获取到现在换，然后用我们自己的那个大模型，然后继承了 CP 功能，看他能获取到到底是什，其实是已经获取到了，对不对，大家可以看到这里，这里面可能是返回的数据格式有问题，然后呢，然后呢会有一点点影响，正常是其实已经获取到了，可以看到吗，当前时间，2025 年 10 月 6 号 21:18:28，大概这样，这里面就返回了，下面是他的一个思考过程，其实这些呃思考过程和一些日志，这个不用管，真正输出的是时间，是这个，当前时间是 2025 年 10 月 6 号，21:18:49，我们调预调了一次对吧，大家能看到能想明白这是什么什么什么结果，就是我之前大模型是没有办法获取这个时间的，那我我把这大模型里面集成了一些工具，我就可以实现这个工具，那扩展说假如说我们有一些其他的需求，大模型没有办法满足，但是我们需要用大模型整合其他的工具，一起去实现，那我们是不是，就可以通过 CP 的这种方式去实现，包括说包括说在 N8N 这个工作流里，也可以集成 MCP 也是一样的，也可以介绍 MCD 客户端，这里面就是把这个地方信息 call 进进对吧，把这个 RL 信息拷贝这里，然后呢 ACB 对吧，都有个传输模式放到这，然后其他的不用管添加进来，然后也可以实现相同，这样这样的话这个工作流他进一步完善，能力更强，然后呢再把这个再把这个测试一下，这里面会有点慢，有飞机流动的那个接口，反正我试下来速度还挺慢的，大家是不是可以看到天气天气查询，查询天气信息已经放出来了，深圳是什么，然后温度是多少，然后湿度多少，风力是多少，这些信息，这些信息是从工具里面查询的，然后呢这个信息是大模型，结合了我查询的信息，然后给出的提示告诉你，白天户外活动注意防晒是吧，早晚擦脚大，这个就是我说了，为什么我们在讲嗯讲 N8N 讲智能体，然后里面要穿插一个 MCP 的一个系列，讲解 CP 这块东西，这么多代码明显更像编程相关的，实际上就是说要知道你单纯的工作流智能体，还要扩展些 MCP 的工具，才能把整个功能变得更完善，所以这块的信息大家有没有什么问题，我看一下我看，to b 调用后台数据库是要开发 API 接口再封闭嗯，MC 嗯，封装厂 MCP 服务是吗，嗯是的是的嗯，有几种方式，有几种方式，第一种方式呢根据你企业的要求，和你企业的一些基础设施不同，有一定的区别，有一种方式呢，是直接就是直接调用那个数据库的，数据库的那个接口，比如说你 MYSQL 还是嗯 progress circle 是吧，各种什么还是 oracle 这种数据库，他们现在应该是有支持 MCP 协议的，支持 MCP 服务的，然后你直接嗯 Python 那个 Python 那个包里，依赖包里面安装那个依赖之后，你可以直接调用数据库，这是一种，那还有一种就是有很多企业呢，它是不允许你直接调用数据库，它会有一个数据公开去丰台的话，除非是他业务主题嗯，如果是数据中台，你是要去使用数据查询一个使用数据的，会有个数据中台，数据中台的话就会自己要封装成 MCP 群，然后豆包的这个用的是哪个模型，我不知道豆包里面内部用的是哪个型，它必然清楚它内部的模型，大模型内部认为不需要调查记录，嗯嗯这个话是这样，人会会怎么就是是这样的，我你你指的这个网页端的话，应该不走 MCP 是指这个是指 DPC 对吧，因为 DPC 这里面本身它只是一个大模型，就是你认为它只有一个大模型，应该没有，应该是没有，应该是没有这个其他的 CP 工具，那你直接通过它去访问，那他是他是没有办法去判断，我要去走走哪个 MCP 工具的，这里面为什么我们我们自己配的那个，你为什么我们自己的代码里，可以去调用工具呢在于两个，一个是皮尔斯，一个是 MCP，后面有个 MCP 服务，就提示词里面我们已经告诉他了，我们现在有哪些工具，对不对，大家可以看到这，其实我们已经，我们已经把这两个工具已经告诉给大模型了，而且大模型本身是支持 function colleague，就是支持函数调用，那它就会根据我的问题，然后判断哎我要调用 query weather 这个方法，然后呢，我要不要传递什么什么参数，对吧嗯，这里面逻辑是什么，回到这里来看，在这问了一个，今天今天深圳天气如何，正面没打日志啊，也打是吧，它里面会收到一个天气查询请求，城市去深圳，他后面后端是做了两回事，首先是大模型告诉你，大模型告诉你不要调用，我这里面应该有打日志，对不对，可以看在这大家可以看下面这个信息，你打日志有打啊，大模型返回信息是我需要查询深圳的天气信息，我们被要求查询是深圳今天的天气信息，但是注意数 barry weather city 参数要求是用英文，因此需要将深圳转化成英文深圳，然后调用 PARAVIER 函数参数为深圳，大家看到了吗，这个这块的内容处理就是大模型去给我们处理，我们没有，我们没有自己去把深圳转化成英文的深圳，但是大模型就把它给处理了，然后呢返回了就是一个是 method，是二，参数组，对，明白什么意思了吗，嗯同时大家可以看一下这个这个其实词这块，这地方这地方这个地方也很重要，当然给大家提到这个地方很重要，就是你要把 MCP 里面的服务，就是 MCP 里面的工具告诉封装成提示词，封装成提示词之后告诉大明星，告诉这个 DESIG 是吧，我有哪些参数可以调用这些参数和这，我有哪些工具可以调用这些工具，它的方法是什么，它要求的参数是什，那当我在提问这个问题的时候，哎大波群众知道我需要这样去处理，呃不是，这是两回事，就是 MCB 底层调用它，它的逻辑是什么，就是简单一点，这边有个客户端是吧，这边有个服务端，300，哎我等我一下，A43，这边有一个客户端，关于客户端对吧，一般一个服务端，I c b u，就是你这边客户端绑定了后面的服务端，有哪些东西之后哎，我只要去调用调用服务端，然后呢，那服务端就会返回一个信息给我，我调用什么方法，什么方法返回去，结果就可以了，然后呢 function calling 是什么意思，就是让大模型具备它，来判断我什么时候要去调用这个方法，就是你这里面可以，这里面可以没有 AI 大模型，这里面我可以这些东西都不要，那我直接在客户端里面去调用一个服务端的，用一个方法可不可以，就是就相当于调用 API，可不可以，也可以直接来调用这个 QUERIZER，也可以，对不对嗯，但是呢我这里面是让大模型这里面 function calling 的，function calling 的意义就在于，让大模型来判断我需不需要调用，调用这个 curry visor，大家还记得我刚才提到两个点，两个智能体的是五个是包 flow，要九点了，Buflow，假如说我的步骤就是第一步随便写随便写，第一步是登录好 APP，第二步是查询天气，第三步是展示天气结果，那我是不是就可以在工作流里面，明确的把这三部列出来，把这三步画出来，然后呢一步步处理就 OK 了，这是 work flow 形式，那那还有一种是 react 的形式，就是我底层有很多工具，我底层有查询天气对吧，我底下有他就进我这边，我我底层还有什么那个用 SA，二制作视频，那 react 的 react 的形式就是，我只需要说，主要的模式是我只需要跟大模型说，帮我，帮我查询查询天气对吧，比如说深圳天气，今天的深圳天气大模型，自己就能判断我应该要做什么事情，对不对，比如说因为那个那个接口里面可以查询的，就是当天的天气，那假如说我要查询昨天天气呢，大模型是不是自己就 react 模式，就是自己会构建，比如说他会先查昨天的时间是，然后用昨天的时间，来查询天气是吧，然后呢，然后呃假如说这是还要第三步要在做什么事，用用昨天的天气制作一个 SARA 短片，就是他自己来对话，就没有一个固定的这个这样的流程，这就是 RENET 总是的，对你如果两个 MCP 的 server 描述很相近的时候，就会有歧义，就会有歧义，有可能就会调用你错误，如果你描述的非常接近，但是里面的功能完全不相同，那就会有歧义，这个就跟我们做编码一样，做 java 也行，或做什么，是不是里面有重写重载，如果你的方法不一样，参数不一样，哎你就要把这个每个参数干上，这样写的很清楚，这样才可以，按下这个 server，里面里面这个这个东西是很重要的这个东西，那么如果你这个东西没有，那这个 MCBA 也是有问题的，所以你所以 super 追问你的那个问题解决了吗，这个一样的呀一样的兄弟，那个 WX 你想想大模型加一些天气插件，也可以实现这些一些天气插件，它不就是 MCP 做的吗，是不是别人别人也是别人做的，只是说这个插件你已经别人已经做好，发布成插件了，然后后面有一台服务一直起着，然后你直接把它集成进去了，那你在 B 端的时候，你在企业中是不是有很多场景，有可能你是没有现成的插件，对不对，需要去开发，所以那个 WX 这个就 OK 了，对吧马哈，你这个问题应该也会了吧，就是做 MCD 的时候，这个这个东西很重要，注释注释很重要，你如果没有或者两个注释完全一模一样，你可能出来的结果就是有问题的，就跟就跟咱代码就是写那程序员一样，如果你之前的前辈给你留了一个代码，然后方法名一模一样，然后注释一模一样，你也懵逼，你也不知道到底应该能用哪一个，对不对，然后呢今天今天还想给大家快速过一下，再过一下那个另外一个 MCP，入门之后还有一个什么呢，就是奖励嗯，偏也是偏低端应用五，就是嗯 graph rub，Grah rog，加一个 MCP，稍等一下，我再，本来 SCP 这块就不介绍了，然后那个 graph rub 嗯，这块介绍一下，就是首先它也是一个 red 的一个技术，也是一个增强检索嗯，这个 rap 的话嗯，我们先说这个 graph rap 为什么要讲这个东西，应应该用到什么场景，就是首先我们经常会提到一个问题，就是现在很多人都体谅说，我们的大模型后面要要挂一个知识库，就只有这样的话，才能解决一些我们企业经营的应用一些问题，确实是这样，就是那为什么要挂一个知识库呢，就是有很多信息有很多内容嗯，你大模型的时候可能是没有对吧，包括你大模型训练的时候，或者是你在公开场所是没有办法获取的信息，那比如说你企业内部的一些生产信息，企业内部的一些数据信息，在大户型上面是没有的对吧，那你只有把这些信息放到一个知识库里面，然后呢大模型挂载这个知识库之后啊，再问企业一些专业的一些问题，或者是处理一些专业的数据的时候，我才能发挥优势，对不对，那里面有哪些场景呢，比如说哪些 rap 比较好用的呢，啊这边给大家写一下，有两个比较好用的，一个是叫 REFLOW，Bblue，一个是 GRAFLA，这个的话 RFLOW 的话应用的比较多，RAFLOW 的话就是一个挂一个知识库，质数库里面，你可以选选那个嗯，模型里面会有嗯有一个 adding 模型，Embedding，Embedding，bedding 模型，这里面也有点有点专业了，就是 EMMANDING 模型就是现代化的模型，这样的话的模型的话嗯，目前用的比较好的比较多的一个是千分三，cl3 里面是有 embedding 模型，然后 deep sick 是没有 embedding 模型，大家要知道，就是 deep sick 没有办法去做那个 embedding，然后呢这是 value 模型的那种千万 3 的模型，或者是一些其他的什么内置的什么 text，比如 embedding gm i jimmini 的这些都都可以，我一般用的就是千位三维引擎，那把这些智识库用嗯，把这知识库，然后把这些知识，用那个 MBEDDING 模型处理完之后呢，你再去查询相关的信息的时候，就可以把智库里面的信息携带出来，然后呢两个两个两个特点是什么，像 rap flow，他这个比较通用，应用场景比较通用，然后呢一个是支持同的模型选择，可以选千问，可以选其他均匀可以选，就是其他的模型，嵌入式模型，然后呢 rag 嗯，graph rag 是什，Grah rag，他更通向更倾向于什么，就是你的整体的知识是有关联的，graph 是图，大家听这名字就知道是图图关系，也是偏编程的，大家如果知道，如果是那个搞编程的，知道什么是图关系，是，减吧这种就是除关系确定性的，这种叫土观行为学，super driver 的话，那个我建议你啊，就如果没有编程经验的话，你可以先先了解智能体，就是先通过已有的工具能解决的问题，然后你发现已有工具解决不了的时候，可以再去看要不要开发一个 MCP 插件，或者谁能帮你开发一个 MCD 插件，然后呢一点点去去去解决这个问题，我不知道那个 super driver，你现在是是是什么样的那个背景，你现在是工作了好多年，还是，是那个上学啊还是什么样背景，然后大家可以先看的，然后刚才那个图的关系是不是已经知道了，就是嗯，是这个图的话，这样对吧，我里面每一个关系它是不确定有几条边的对吧，那有可能是两条边，有可能五条边，也有可能是一条边关联，有个关联关系，他这个图他这个关系就比较复杂，比较简单了，典型的还有个数关系对吧，一层一层一层一层，主要是二二级分差不多级分差的数，那这种求关系的话，就是如果你用文件关联性很强，里面的一些逻辑点很强，那你就比较适合用这个关系，比如说人和人之间的关系是吧，张三李四和王五，然后呢他俩之间是什么夫妻关系，他俩之间是什么教师关系，他俩之间是什么关系，然后我一查诶，他们通过这个人把这些关系全部查出来，这种话比较适合应对关系，同样的 graph rog 也是一样的，如果你的一些资料里面有很多资料，希望通过这些资料把他们内在的关系建立起来，那你就用这个 GRAFRAP 效流，如果你就是一个比较零散，或者说是典型的一些零散的知识点，文章内容或者比较少知识或知识比较少，不需要什么复杂的一些关联关系，那你就用 rap flow，这比较常用一个，GRAPRAP 的话，我我自己我自己觉得他有点对，标什么标，4G 就是我们以前传统编程的时候，会有个属性关系，New sj，它就类似于这个 new sj 一样，然后只是说把这个关系变成了知识库，因为 4G 的话是处于一个关系管理无关系，那个不是 rap，是无关系管理里面增加了知识互助的申请，我是这么理解的，大家可以纠正，OK 这里面会看到了一些一些使用的一些场景，包括一些信息支持的方式，包括全局搜索，局部局部搜索是吧些 Deft 的搜索，然后呢 graph rag 的一个运行流程，比如说索引过程，索引过程，然后查询过程，然后提示词性化这些，大家过一下就可以，底层呢这就是它的些安装说 pig install grass rap，然后呢安装完之后呢，创建数据集嗯，graph lag 的话目前只支持两种数据集，一个是 text，一个 CSV，所以你也有些复杂，比如说你如果拿到了一个 PDF 那个文件，那你需要把它处理处理完，转化成 text 和 CSV 的信息文件才可以，这个文件的话是底下底下这个事示例是什么呢，是我之前我们在做了一个，AI 问诊项目的一个数据集，我直接用这个数据集做一个测试，跟数据集相关的放在，但是内容不多，所以呢所以呢说实话，你用这个 graph rag 和 rap flow，看不出太大的差别，因为内容就这么点，没有太多信息，这里面会讲啊 graph rag 的线安装方式，比如说创建数据集，然后创建目录啊，里面的一些然数化构建，这里面要提一下这个初始化构建的话，一定是在那个 Python 的 Python 的虚拟环境里面去构建，不要直接在命令，意思呢大家看一下，嗯这是 PX，然后大家用其他也可以 CMD，不一样，这里面就是一样激活一个页，激活一个 Python 虚拟环境，放大接一，然后呢在这个环境里面去，这个是虚拟环境里面去执行或执行这个创建，因为我已经安装了，我安装了这个，既然装去执行那个创建，擦掉一个，就把今天的创建过，对已经创建好加，不，这个创建个环境里面还有一些信息，也是词的相关方配置文件，相关的配置文件，然后去去做调整之后都讲这就是创建完了啊，这些地方你去修改里面就都有注释，就有注释上面，比如说你这个是这个是我本地的大模型地址，可以换到线上大模型，包括这个这个地址都可以换，包括 KE，这底下就是换成那个 RUFLO，这底下就换成了危机流动的，而对一些配置项些说明，然后呢，正常情况下弄完之后，然后新建一批 put import，就是你把你的文件放到这里面来，放到这里面来，然后呢再执行 index din 进行进行建索引的过程，建完索引之后再去进行一些查询，就会有一些你先这个关联到知识库了，可以看一下这个，嗯会有点暗，忘了把换成硅基，忘了把行李箱里就行，这个这个这个估计流动太大了，这里面也是有对应的代码，都有对应的代码，大家可以参照这个代码去看和一些日志流程啊，建建索引的时候，这些日志就可以进去看，包括 CP 搭建的相同分同体这块内容，再也不去了解一下这块呢，嗯我给大家分享一些东西，就这块已经在处理了，就是让他先处理，我跟大家讲讲一下，我给大家讲这些东西呢，就是比较偏向于低端的，因为我们比比如说经常接触一些项目，最近接了一个项目，也切切实实用到了能解决一些问题，比如说 MCP 对他们，比如说那个我很 man，看我这里面的班虽然比较简单，虽然比较简单，但是都是些解决问题的，看这个看这个，像我这个简单的构建了一个一个，工作流的证明体，但是呢解决实际问题，比如说这里面先用了千万的大模型，千万的 VR max 大模型进行图片识别，一般识别完之后进行输出，格式化和输出格式化之后再去用一些提示词啊，进行一些对吧，进行输出啊，进行那个你说些评估答案，这都是我们实际应用到的一些项目，像那个其他其他网上这种，讲实际项目应该比较少，这块其实如果大家如果是偏技术的，然后后面要用用那个 N8N 也好，就是智能体进行低端企业端的一个项目研发，那你多看看这个领域智能体搭建，然后呢 RFLOW 或者 MCP 这块东西，大家可以多看一下，最开始可以啊，最开始能，说说这个吧，这个的话这个话为什么没放进去，因为这个这个这个东西跟其他教练的那个内容，是重合的，类似的，就就无非是这个是招聘网站，其他的可能什么其他网站流程都是一样，就是获取招聘网站的信息，然后呢 AI 解析为标准结构化数据，然后再拆分再显示次数，多一个内容是就是逻辑是一样，只是他们可能是呃获取别的网站的信息，最终写入的就是这个是招聘形式数，这里面介绍了，下班能安装 windows 上的比较快捷的方式，大家如果 docker 这块卡的太久了，docker 官网，然后就在这 docker 文班这块就通过 docker 学到 N 班的话，卡的太久的话，可以先用那个录了 JS 的方式，因为那个 docker 的方式有点复杂，一个是标准本身就是偏偏命名的菜，菜懂不懂不懂，明文的不太懂，当然后呢另外是 docker，他那个那个镜像地址变更之后，各种网络环境去搞个梯子啊，搞那个网络啊，特别麻烦，特别 N8NNN8N 的那个网络很难搞，如果这块搞定的话，您可以用这个方式去去试一下，因为大家肯定要先先上手，先上手以后，后面真正需要的时候再训 docker，这个挺好的，docker 这种偏生产方式的区，这个网站我之前我已经分享了，你想大家直接可以访问，然后大家那个针对今天的直播，今天的分享有没有什么需要问的问题，因为我觉得我觉得我自己讲下来的感觉，应该还是有一些难度的，有一些有些同学可能听不懂，还有什么问题可以提问，一，OKOK 没问题，我一会发到群里讲讲，大家都在那两个群里是吧，大家应该是想看这个是吧，我想看这个这个文档可以啥，应该是模型模型管理比赛是吧，模型管理平台，一个是 VIM，还有一个叫什么拆封，还有就 VLM 其实就可以了，VIM 已经非常强大了，企业级用那个就可以，如果也是分级别的，如果如果你企业只是在测试的情况下，不要去用那个布拉玛就可以，布拉玛和 i m studio 就可以，如果你真正企业级应用，真正是企业需要去搭建的啊，本地的大模型大模型平台有几台，什么华为一体机，或者是什么 V100HH100M 的机器，那你就要考虑用那个 VRMVL，然后那个其他的点我想到就跟大家讲，然后那个大家如果在实际过程中有什么问题嗯，都可以去找我的，这两天对，这两天可能会刚好刚好带完，可能会消息恢复没那么及时，但是我看到了就会给大家回复，大家也可以私聊我，嗯其实都想看这个我这个破局的这几篇文章，这三篇文章这两个已经放放到那个已经看到了，就这两个就这两个合到那个文档里面，写了训练营手册，就这样，这文班的这个我再单独分享出这个这个内容，其实跟其他教练是类似的，所以就没放进，OK 那没什么问题的话，就就今天就先到这里吧，再次感谢大家，然后又又耽误了大家，也耗了大概两个小时，延迟了一个多小时，OKOK 我本来计划本来计划一个半小时讲完，延迟了半个多小时，半个小时，今天还是中秋节啊，这么多人在线，非常感谢大家，大家中秋快乐，大家有什么问题的话，就那个找我，其实我觉得讲的还是比较快的，大家可能没跟上，大家那个下来自己一定要跟着跑一遍，大家在跑的过程中肯定也会遇到问题，遇到问题的时候，先想我给大家说的怎么解决这个问题，看看能不能先用过大模型去解决，为什么呢，如果你通过大博士自己解决这个问题的话，就相当于把这个知识变成自己，你如果一直依赖于别人去给你回答，那你你其实不一定能真正理解这个问题的本质，大家一定要先去尝试解决这个问题，解决问题的能力也是一种的，也是能力的一种，好了好了，再一次感谢大家，今天就到这儿了啊拜拜
